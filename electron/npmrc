# Always save exact versions when running `npm install <pkg>` —
# prevents future contributors from accidentally reintroducing
# caret ranges that `npm audit fix --force` can exploit to jump majors.
save-exact=true

# Fail loudly on peer dependency conflicts instead of silently
# choosing a (possibly broken) resolution.
legacy-peer-deps=false
